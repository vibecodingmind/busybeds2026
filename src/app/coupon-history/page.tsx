'use client'; import { useEffect, useState } from 'react'; import { Card, CardContent } from '@/components/ui/card'; import { Badge } from '@/components/ui/badge'; import { useAuth } from '@/context/AuthContext'; import { useRouter } from 'next/navigation'; import { Ticket } from 'lucide-react'; import type { Coupon } from '@/types';
export default function CouponHistoryPage() { const { user, loading: al } = useAuth(); const router = useRouter(); const [coupons, setCoupons] = useState<Coupon[]>([]);
  useEffect(() => { if(!al&&!user){router.push('/login');return;} if(user) fetch('/api/coupons?limit=100').then(r=>r.json()).then(d=>setCoupons(d.data||[])); }, [user,al]);
  return (<div className="container mx-auto px-4 py-8"><h1 className="text-3xl font-bold mb-6">Coupon History</h1>{coupons.length>0?<div className="space-y-2">{coupons.map(c=>(<Card key={c.id} className="p-3"><div className="flex items-center justify-between text-sm"><div className="font-mono">{c.code}</div><div className="flex items-center gap-2"><Badge>{c.discountPercent}% OFF</Badge><Badge className={c.status==='active'?'bg-emerald text-emerald-foreground':c.status==='redeemed'?'bg-gold text-gold-foreground':'bg-muted'}>{c.status}</Badge></div></div></Card>))}</div>:<p className="text-muted-foreground text-center py-8">No coupons yet</p>}</div>); }
