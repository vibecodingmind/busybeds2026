'use client'; import { Card, CardContent } from '@/components/ui/card'; import { Button } from '@/components/ui/button'; import { Input } from '@/components/ui/input'; import { Label } from '@/components/ui/label'; import { Textarea } from '@/components/ui/textarea'; import { useState } from 'react'; import { toast } from 'sonner';
export default function OwnerEditPage() { const [form, setForm] = useState({ name: '', descriptionShort: '', descriptionLong: '', discountPercent: 15 });
  const save = async () => { toast.success('Hotel updated!'); };
  return (<div className="container mx-auto px-4 py-8 max-w-2xl"><h1 className="text-3xl font-bold mb-6">Edit Hotel</h1><Card className="p-6"><div className="space-y-4"><div><Label>Hotel Name</Label><Input value={form.name} onChange={e => setForm(f=>({...f,name:e.target.value}))} /></div><div><Label>Short Description</Label><Input value={form.descriptionShort} onChange={e => setForm(f=>({...f,descriptionShort:e.target.value}))} /></div><div><Label>Full Description</Label><Textarea rows={4} value={form.descriptionLong} onChange={e => setForm(f=>({...f,descriptionLong:e.target.value}))} /></div><div><Label>Discount %</Label><Input type="number" value={form.discountPercent} onChange={e => setForm(f=>({...f,discountPercent:parseInt(e.target.value)||15}))} /></div><Button className="bg-emerald hover:bg-emerald/90 text-emerald-foreground" onClick={save}>Save Changes</Button></div></Card></div>); }
